import { EdgeInsetsPropType, StyleSheet } from "react-native";

import colors from "./colors";

export default StyleSheet.create({
  bodyText: {
    fontFamily: "open-sans",
    color: colors.primary,
  },
});
